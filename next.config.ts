import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Allows the dev server to handle requests from your LAN IP (10.141.x.x)
  allowedDevOrigins: ['10.141.229.239'],


};

export default nextConfig;
